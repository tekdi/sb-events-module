import { ComponentFactoryResolver, OnInit, ViewContainerRef, Renderer2, ElementRef } from '@angular/core';
import { FormGroup, FormControl } from '@angular/forms';
import { FieldConfig } from '../common-form-config';
import { Subject } from 'rxjs';
export declare class DynamicFieldDirective implements OnInit {
    private resolver;
    private container;
    private renderer2;
    private elementRef;
    field: FieldConfig<String>;
    formGroup: FormGroup;
    formControlRef?: FormControl;
    disabled?: boolean;
    label: String;
    options?: any;
    placeholder?: string;
    context?: FormControl;
    validations?: any;
    contextTerms?: any;
    depends?: FormControl[];
    dependencyTerms?: any;
    default: any;
    dataLoadStatusDelegate: Subject<'LOADING' | 'LOADED'>;
    componentRef: any;
    constructor(resolver: ComponentFactoryResolver, container: ViewContainerRef, renderer2: Renderer2, elementRef: ElementRef);
    ngOnInit(): void;
    setClassListOnElement(cssClasses: any): void;
}
