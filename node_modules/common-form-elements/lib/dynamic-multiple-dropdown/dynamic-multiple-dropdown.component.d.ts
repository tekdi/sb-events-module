import { ChangeDetectorRef, OnChanges, OnDestroy, OnInit, SimpleChanges } from '@angular/core';
import { FormControl, FormGroup } from '@angular/forms';
import { Subject } from 'rxjs';
import { FieldConfig } from '../common-form-config';
import { fromJS, List, Map, Set } from 'immutable';
export declare class DynamicMultipleDropdownComponent implements OnInit, OnChanges, OnDestroy {
    private changeDetectionRef;
    disabled?: boolean;
    field: FieldConfig<String>;
    options: any;
    label?: string;
    labelHtml: any;
    placeholder?: string;
    isMultiple: boolean;
    context?: FormControl;
    formControlRef?: FormControl;
    formGroup?: FormGroup;
    platform: any;
    default?: any;
    contextData: any;
    dataLoadStatusDelegate: Subject<'LOADING' | 'LOADED'>;
    depends?: FormControl[];
    dependencyTerms?: any;
    _: any;
    isDependsInvalid: any;
    showModal: boolean;
    tempValue: Set<any>;
    resolvedOptions: List<Map<string, string>>;
    optionValueToOptionLabelMap: Map<any, string>;
    fromJS: typeof fromJS;
    private dispose$;
    docClick(): void;
    constructor(changeDetectionRef: ChangeDetectorRef);
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    onSubmit(): void;
    openModal(event: any): void;
    addSelected(option: Map<string, string>): void;
    onCancel(): void;
    ngOnDestroy(): void;
    private isOptionsArray;
    private isOptionsClosure;
    private isOptionsMap;
    private setTempValue;
    private resetTempValue;
    private setupOptions;
}
