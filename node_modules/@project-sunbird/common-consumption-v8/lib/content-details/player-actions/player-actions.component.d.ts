import { OnInit, EventEmitter } from '@angular/core';
import { IActionButton, ActionButtonType, IButtonClick } from './model';
export declare class PlayerActionsComponent implements OnInit {
    actionButtons: IActionButton[];
    buttonClick: EventEmitter<IButtonClick>;
    showFullscreen: boolean;
    fullscreenButton: IActionButton;
    readonly ActionButtonType: typeof ActionButtonType;
    ngOnInit(): void;
    /**
     * Emits event when user clicks on the button
     * @param event Contains default mouse click event
     * @param actionButton Data for the clicked button
     */
    onButtonClick(event: MouseEvent, actionButton: IActionButton): void;
}
