import { EventEmitter, OnInit } from '@angular/core';
import { DomSanitizer, SafeResourceUrl } from '@angular/platform-browser';
export interface IPreviewActionButton {
    label: string;
    name: string;
    show: boolean;
}
export interface IPreviewActionButtonClick {
    event: MouseEvent;
    name: string;
}
export declare class CertificateActionsComponent implements OnInit {
    domSanitizer: DomSanitizer;
    select: IPreviewActionButton;
    preview: IPreviewActionButton;
    remove: IPreviewActionButton;
    template: string;
    isSelectedTemplate: boolean;
    actionButtons: IPreviewActionButton[];
    buttonClick: EventEmitter<IPreviewActionButtonClick>;
    showOverlay: boolean;
    templateUrl: SafeResourceUrl | string;
    constructor(domSanitizer: DomSanitizer);
    onButtonClick(event: MouseEvent, name: string): void;
    ngOnInit(): void;
}
